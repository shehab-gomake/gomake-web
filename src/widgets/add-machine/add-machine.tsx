import {useStyle} from "@/widgets/add-machine/style";

const AddMachine = () => {
    const {classes} = useStyle();
    return (
        <div>
            <h2>Add printing machine</h2>
        </div>
    );
}
export {AddMachine}