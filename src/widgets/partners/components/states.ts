import { atom } from "recoil";

export const blockModalState = atom({
  key: "blockModalState",
  default: false
});

export const partnerInfoModalState = atom({
  key: "partnerInfoModalState",
  default: false
});
