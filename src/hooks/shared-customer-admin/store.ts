import { atom } from "recoil";

export const digitslPriceState = atom({
  key: "digitslPriceState",
  default: {},
});


export const checkParameterState = atom({
  key: "checkParameterState",
  default: false,
});