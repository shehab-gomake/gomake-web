import { atom } from "recoil";

export const isUpdateContactNameState = atom({
  key: "isUpdateContactNameState",
  default: null,
});
export const isUpdateContactEmailState = atom({
  key: "isUpdateContactEmailState",
  default: null,
});
export const isUpdateContactMobileState = atom({
  key: "isUpdateContactMobileState",
  default: null,
});
